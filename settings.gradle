pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode = RepositoriesMode.PREFER_SETTINGS
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "alkosmen"
include("common")
include("androidapp")
project(":common").projectDir = file("shared")
project(":androidapp").projectDir = file("androidApp")
